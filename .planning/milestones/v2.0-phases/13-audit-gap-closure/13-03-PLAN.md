---
phase: 13-audit-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/system/disko.nix
  - modules/system/boot.nix
  - modules/system/impermanence.nix
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - "modules/system/disko.nix header contains `# Profiles : host layout=btrfs | server layout=lvm`"
    - "modules/system/boot.nix header contains `# Profiles : host | server`"
    - "modules/system/impermanence.nix header contains `# Profiles : host mode=btrfs | server mode=full`"
    - "In each file the `# Profiles :` line appears inside the comment block, after the description line, before the blank separator line that precedes the function args"
  artifacts:
    - path: "modules/system/disko.nix"
      provides: "Profiles cross-reference in header"
      contains: "# Profiles : host layout=btrfs | server layout=lvm"
    - path: "modules/system/boot.nix"
      provides: "Profiles cross-reference in header"
      contains: "# Profiles : host | server"
    - path: "modules/system/impermanence.nix"
      provides: "Profiles cross-reference in header"
      contains: "# Profiles : host mode=btrfs | server mode=full"
  key_links:
    - from: "module header comment block"
      to: "flake.nix profiles (host, server)"
      via: "# Profiles : cross-reference line in each header"
      pattern: "# Profiles :"
---

<objective>
Add `# Profiles :` cross-reference comment lines to the section headers of `disko.nix`, `boot.nix`, and `impermanence.nix`, linking each module to the profiles that use it.

Purpose: PROF-03 requires section-header comments on these three modules to be updated to reflect new options and profile cross-references. The cross-reference tells readers which profiles activate which layout/mode without requiring them to read `flake.nix`.
Output: Three module files updated with a `# Profiles :` line in each header's comment block.
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current header structure for all three files. The # Profiles : line inserts after
     the description line (line 3) and before the blank separator line (line 4). -->

Current disko.nix lines 1–5:
```
1: # modules/system/disko.nix
2: #
3: # Declarative disk layout and layout-conditional initrd config. btrfs (desktop) or lvm (server). No default — must be set explicitly.
4: (blank line)
5: { config, lib, pkgs, ... }:
```

Target disko.nix lines 1–6:
```
1: # modules/system/disko.nix
2: #
3: # Declarative disk layout and layout-conditional initrd config. btrfs (desktop) or lvm (server). No default — must be set explicitly.
4: # Profiles : host layout=btrfs | server layout=lvm
5: (blank line)
6: { config, lib, pkgs, ... }:
```

Current boot.nix lines 1–5:
```
1: # modules/system/boot.nix
2: #
3: # Layout-agnostic initrd and bootloader (systemd-boot + EFI). Layout-specific initrd config lives in disko.nix.
4: (blank line)
5: { pkgs, ... }:
```

Target boot.nix lines 1–6:
```
1: # modules/system/boot.nix
2: #
3: # Layout-agnostic initrd and bootloader (systemd-boot + EFI). Layout-specific initrd config lives in disko.nix.
4: # Profiles : host | server
5: (blank line)
6: { pkgs, ... }:
```

Current impermanence.nix lines 1–5:
```
1: # modules/system/impermanence.nix
2: #
3: # Selective persistence via environment.persistence bind-mounts. btrfs mode for desktop; full mode for server.
4: (blank line)
5: { config, lib, pkgs, ... }:
```

Target impermanence.nix lines 1–6:
```
1: # modules/system/impermanence.nix
2: #
3: # Selective persistence via environment.persistence bind-mounts. btrfs mode for desktop; full mode for server.
4: # Profiles : host mode=btrfs | server mode=full
5: (blank line)
6: { config, lib, pkgs, ... }:
```

CRITICAL: The blank separator line (between header block and function args) must be preserved.
The `# Profiles :` line inserts BEFORE the blank line, not after it.
After insertion: line 3 = description, line 4 = `# Profiles :`, line 5 = blank, line 6 = function args.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert # Profiles : lines into disko.nix, boot.nix, and impermanence.nix headers</name>
  <files>modules/system/disko.nix, modules/system/boot.nix, modules/system/impermanence.nix</files>
  <action>
Read each file in full before editing. For each file, insert one line into the header block immediately after the description line (line 3) and before the blank separator line. The blank separator line must remain between the `# Profiles :` line and the function args.

**modules/system/disko.nix:**
After line 3 (`# Declarative disk layout and layout-conditional initrd config. btrfs (desktop) or lvm (server). No default — must be set explicitly.`), insert:
```
# Profiles : host layout=btrfs | server layout=lvm
```
The result: line 3 = description, line 4 = `# Profiles : host layout=btrfs | server layout=lvm`, line 5 = blank, line 6 = `{ config, lib, pkgs, ... }:`.

**modules/system/boot.nix:**
After line 3 (`# Layout-agnostic initrd and bootloader (systemd-boot + EFI). Layout-specific initrd config lives in disko.nix.`), insert:
```
# Profiles : host | server
```
The result: line 3 = description, line 4 = `# Profiles : host | server`, line 5 = blank, line 6 = `{ pkgs, ... }:`.

**modules/system/impermanence.nix:**
After line 3 (`# Selective persistence via environment.persistence bind-mounts. btrfs mode for desktop; full mode for server.`), insert:
```
# Profiles : host mode=btrfs | server mode=full
```
The result: line 3 = description, line 4 = `# Profiles : host mode=btrfs | server mode=full`, line 5 = blank, line 6 = `{ config, lib, pkgs, ... }:`.

Do NOT modify any other lines in any of the three files. The body of each module (the Nix expression below the header) must be untouched.

After writing all three files, verify the first 6 lines of each match the target structure shown above.
  </action>
  <verify>
    <automated>grep "# Profiles :" /home/demon/Developments/nerv.nixos/modules/system/disko.nix /home/demon/Developments/nerv.nixos/modules/system/boot.nix /home/demon/Developments/nerv.nixos/modules/system/impermanence.nix</automated>
  </verify>
  <done>All three module files contain a `# Profiles :` line; disko.nix has `host layout=btrfs | server layout=lvm`; boot.nix has `host | server`; impermanence.nix has `host mode=btrfs | server mode=full`; blank separator line preserved between `# Profiles :` line and function args in each file.</done>
</task>

</tasks>

<verification>
Run after task complete:

```bash
grep -n "# Profiles :" /home/demon/Developments/nerv.nixos/modules/system/disko.nix \
  /home/demon/Developments/nerv.nixos/modules/system/boot.nix \
  /home/demon/Developments/nerv.nixos/modules/system/impermanence.nix
```

Expected output (3 lines):
```
modules/system/disko.nix:4:# Profiles : host layout=btrfs | server layout=lvm
modules/system/boot.nix:4:# Profiles : host | server
modules/system/impermanence.nix:4:# Profiles : host mode=btrfs | server mode=full
```

Confirm blank separator still present:
```bash
sed -n '4,6p' /home/demon/Developments/nerv.nixos/modules/system/disko.nix
```
Expected: `# Profiles : host layout=btrfs | server layout=lvm`, blank line, `{ config, lib, pkgs, ... }:`.
</verification>

<success_criteria>
- `modules/system/disko.nix` line 4: `# Profiles : host layout=btrfs | server layout=lvm`
- `modules/system/boot.nix` line 4: `# Profiles : host | server`
- `modules/system/impermanence.nix` line 4: `# Profiles : host mode=btrfs | server mode=full`
- Blank separator line preserved between each `# Profiles :` line and the file's function args
- No other lines modified in any of the three files
- PROF-03: Module headers cross-reference which profiles use which layout/mode
</success_criteria>

<output>
After completion, create `.planning/phases/13-audit-gap-closure/13-03-SUMMARY.md`
</output>
