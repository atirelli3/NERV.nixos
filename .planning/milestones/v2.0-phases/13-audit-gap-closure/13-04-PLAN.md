---
phase: 13-audit-gap-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/system/default.nix
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - "modules/system/default.nix line 13 comment reads: `# declarative disk layout (btrfs/lvm) with layout-conditional initrd services`"
    - "The stale phrase `conditional LVM LVs based on impermanence mode` no longer appears in default.nix"
  artifacts:
    - path: "modules/system/default.nix"
      provides: "accurate disko.nix import comment"
      contains: "declarative disk layout (btrfs/lvm) with layout-conditional initrd services"
  key_links:
    - from: "modules/system/default.nix line 13"
      to: "modules/system/disko.nix"
      via: "import comment describing what the module does"
      pattern: "declarative disk layout"
---

<objective>
Fix the stale inline comment on the `disko.nix` import line in `modules/system/default.nix` (line 13) to accurately describe what the module does in v2.0.

Purpose: The existing comment `# declarative disk layout — conditional LVM LVs based on impermanence mode` is incorrect since Phase 9: disko.nix now branches on `nerv.disko.layout` (not impermanence mode), and the v2.0 module also contains layout-conditional initrd services. The new comment accurately reflects the module's current behavior. This is a documentation accuracy fix under PROF-03.
Output: `modules/system/default.nix` line 13 updated with the correct comment.
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current modules/system/default.nix — all 16 lines. -->

```nix
# modules/system/default.nix
#
# Aggregates all nerv system modules. secureboot.nix must be last — applies lib.mkForce false on systemd-boot.
{ imports = [
    ./identity.nix
    ./hardware.nix
    ./kernel.nix
    ./security.nix
    ./nix.nix
    ./packages.nix      # base system packages shipped on all flavors (git, fastfetch)
    ./boot.nix          # initrd + LUKS + bootloader (opaque)
    ./impermanence.nix  # selective per-directory tmpfs (enable = false by default)
    ./disko.nix         # declarative disk layout — conditional LVM LVs based on impermanence mode
    ./secureboot.nix    # Lanzaboote + TPM2 — must be last (lib.mkForce false on systemd-boot)
  ];
}
```

Line 13 current:
```nix
    ./disko.nix         # declarative disk layout — conditional LVM LVs based on impermanence mode
```

Line 13 target (change comment only, preserve whitespace alignment):
```nix
    ./disko.nix         # declarative disk layout (btrfs/lvm) with layout-conditional initrd services
```

The leading whitespace (4 spaces + `./disko.nix` + spacing to comment) must be preserved exactly as-is. Only the comment text after `# ` changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale comment on disko.nix import line in modules/system/default.nix</name>
  <files>modules/system/default.nix</files>
  <action>
Read `modules/system/default.nix` in full (16 lines). Locate line 13:
```
    ./disko.nix         # declarative disk layout — conditional LVM LVs based on impermanence mode
```

Replace the comment text (the portion after `#`) with:
```
 declarative disk layout (btrfs/lvm) with layout-conditional initrd services
```

The full line 13 after the change must be:
```
    ./disko.nix         # declarative disk layout (btrfs/lvm) with layout-conditional initrd services
```

Preserve:
- The 4-space indentation before `./disko.nix`
- The spacing between `./disko.nix` and `#` (it uses spaces to column-align comments with neighboring lines — count exact spaces from the original)
- All other lines unchanged

Do NOT modify any other line in the file.
  </action>
  <verify>
    <automated>grep -n "declarative disk layout" /home/demon/Developments/nerv.nixos/modules/system/default.nix</automated>
  </verify>
  <done>modules/system/default.nix line 13 reads `./disko.nix         # declarative disk layout (btrfs/lvm) with layout-conditional initrd services`; the phrase "conditional LVM LVs based on impermanence mode" no longer appears in the file; no other lines changed.</done>
</task>

</tasks>

<verification>
Run after task complete:

```bash
grep -n "disko" /home/demon/Developments/nerv.nixos/modules/system/default.nix
```

Expected output:
```
13:    ./disko.nix         # declarative disk layout (btrfs/lvm) with layout-conditional initrd services
```

Confirm stale phrase is gone:
```bash
grep "impermanence mode" /home/demon/Developments/nerv.nixos/modules/system/default.nix
```
Expected: no output (empty).
</verification>

<success_criteria>
- `modules/system/default.nix` line 13 comment reads exactly: `# declarative disk layout (btrfs/lvm) with layout-conditional initrd services`
- The phrase `conditional LVM LVs based on impermanence mode` does not appear anywhere in the file
- All other lines in the file are unchanged
- PROF-03: Module documentation accurately describes current behavior (layout branching on nerv.disko.layout, not impermanence mode)
</success_criteria>

<output>
After completion, create `.planning/phases/13-audit-gap-closure/13-04-SUMMARY.md`
</output>
