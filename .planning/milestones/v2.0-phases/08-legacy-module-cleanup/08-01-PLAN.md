---
phase: 08-legacy-module-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/openssh.nix
  - modules/pipewire.nix
  - modules/bluetooth.nix
  - modules/printing.nix
  - modules/zsh.nix
  - modules/kernel.nix
  - modules/security.nix
  - modules/nix.nix
  - modules/hardware.nix
autonomous: true
requirements:
  - dead-modules-cleanup
must_haves:
  truths:
    - "No .nix files remain in the modules/ root directory (only the subdirectories system/ and services/ remain)"
    - "nix flake check --no-build passes after deletion (no evaluation errors introduced)"
  artifacts:
    - path: "modules/default.nix"
      provides: "Top-level aggregator — imports ./system ./services ../home (unchanged)"
      contains: "imports"
  key_links:
    - from: "modules/default.nix"
      to: "modules/system and modules/services"
      via: "imports list (./system ./services ../home)"
      pattern: "imports = \\[.*\\./system.*\\./services"
---

<objective>
Delete 9 dead flat module files from the modules/ root directory. These files (openssh.nix, pipewire.nix, bluetooth.nix, printing.nix, zsh.nix, kernel.nix, security.nix, nix.nix, hardware.nix) are superseded by their structured counterparts in modules/services/ and modules/system/ respectively. No aggregator imports them — deletion is safe.

Purpose: Eliminate dead code that would confuse users of the NERV.nixos repo by creating ambiguity about which module is authoritative.
Output: Clean modules/ root with only default.nix and two subdirectories (system/, services/).
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-legacy-module-cleanup/08-VALIDATION.md

<interfaces>
<!-- Confirmed by direct inspection: modules/default.nix does NOT import any flat modules/*.nix -->
<!-- modules/default.nix imports: [ ./system ./services ../home ] -->
<!-- modules/services/default.nix imports: [ ./openssh.nix ./pipewire.nix ./bluetooth.nix ./printing.nix ./zsh.nix ] (structured versions in services/) -->
<!-- modules/system/default.nix imports: [ ./identity.nix ./hardware.nix ./kernel.nix ./security.nix ./nix.nix ./boot.nix ./impermanence.nix ./secureboot.nix ] (structured versions in system/) -->
<!-- Conclusion: flat modules/*.nix are completely unreferenced — safe to delete -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 9 dead flat module files</name>
  <files>
    modules/openssh.nix
    modules/pipewire.nix
    modules/bluetooth.nix
    modules/printing.nix
    modules/zsh.nix
    modules/kernel.nix
    modules/security.nix
    modules/nix.nix
    modules/hardware.nix
  </files>
  <action>
    Delete each of the 9 flat module files in the modules/ root directory using the Bash tool:

    ```
    rm /home/demon/Developments/test-nerv.nixos/modules/openssh.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/pipewire.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/bluetooth.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/printing.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/zsh.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/kernel.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/security.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/nix.nix
    rm /home/demon/Developments/test-nerv.nixos/modules/hardware.nix
    ```

    These files are superseded by their structured counterparts:
    - modules/openssh.nix → modules/services/openssh.nix
    - modules/pipewire.nix → modules/services/pipewire.nix
    - modules/bluetooth.nix → modules/services/bluetooth.nix
    - modules/printing.nix → modules/services/printing.nix
    - modules/zsh.nix → modules/services/zsh.nix
    - modules/kernel.nix → modules/system/kernel.nix
    - modules/security.nix → modules/system/security.nix
    - modules/nix.nix → modules/system/nix.nix
    - modules/hardware.nix → modules/system/hardware.nix

    Do NOT modify modules/default.nix, modules/system/, or modules/services/ — they are correct.

    After deletion, verify the modules/ root contains only: default.nix, system/, services/
    ```
    ls /home/demon/Developments/test-nerv.nixos/modules/
    ```
  </action>
  <verify>
    <automated>ls /home/demon/Developments/test-nerv.nixos/modules/*.nix 2>/dev/null | wc -l</automated>
  </verify>
  <done>Command above returns 1 (only modules/default.nix remains — no flat *.nix files). The modules/ root contains exactly: default.nix, system/, services/</done>
</task>

<task type="auto">
  <name>Task 2: Verify flake evaluates cleanly after deletion</name>
  <files></files>
  <action>
    Run nix flake check --no-build from the repo root to confirm no evaluation errors were introduced by the deletion. Since modules/default.nix only imports ./system and ./services (not the flat files), this should pass cleanly.

    ```
    cd /home/demon/Developments/test-nerv.nixos && nix flake check --no-build 2>&1 | tail -10
    ```

    If any error mentions a missing module file (e.g., "could not find openssh.nix"), that indicates something unexpectedly imports a flat module. Inspect the import chain and fix before proceeding. Expected: no errors.

    Commit the deletions:
    ```
    git -C /home/demon/Developments/test-nerv.nixos add -u modules/
    git -C /home/demon/Developments/test-nerv.nixos commit -m "chore(08-01): delete 9 dead flat modules superseded by modules/system/ and modules/services/"
    ```
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix flake check --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>nix flake check --no-build exits 0 with no error output. Git commit created.</done>
</task>

</tasks>

<verification>
After both tasks:
- `ls modules/*.nix 2>/dev/null | wc -l` returns 1 (only default.nix)
- `nix flake check --no-build` exits 0
- `git log --oneline -1` shows the deletion commit
</verification>

<success_criteria>
1. modules/ root contains only default.nix, system/, services/ — no flat *.nix files
2. nix flake check --no-build passes cleanly
3. modules/default.nix is unchanged (still imports ./system ./services ../home)
</success_criteria>

<output>
After completion, create `.planning/phases/08-legacy-module-cleanup/08-01-SUMMARY.md`
</output>
