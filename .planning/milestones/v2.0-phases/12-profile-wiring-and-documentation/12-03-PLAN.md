---
phase: 12-profile-wiring-and-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PROF-04

must_haves:
  truths:
    - "README.md contains a '### B — BTRFS layout (hostProfile)' installation section"
    - "The BTRFS section explicitly documents: disko run → @root-blank snapshot → nixos-install in that order"
    - "The @root-blank snapshot command appears verbatim: btrfs subvolume snapshot -r /mnt/@ /mnt/@root-blank"
    - "The vm row is absent from the Profiles table"
    - "The impermanence description no longer mentions 'minimal' mode"
    - "Repository Layout section shows modules/system/disko.nix, not hosts/disko-configuration.nix"
    - "modules/system/impermanence.nix appears in the Repository Layout with a BTRFS/full mode description"
    - "The boot.nix description in Repository Layout reads 'layout-agnostic initrd + bootloader'"
  artifacts:
    - path: "README.md"
      provides: "BTRFS install section, updated Profiles table, updated Repository Layout, corrected impermanence description"
      contains: "@root-blank"
  key_links:
    - from: "README Section B step sequence"
      to: "disko.nix LUKS section @root-blank note"
      via: "prose reference to mandatory pre-nixos-install snapshot"
      pattern: "root-blank"
---

<objective>
Update README.md with four distinct changes:

1. Add `### B — BTRFS layout (hostProfile)` installation subsection (PROF-04) — full walkthrough mirroring Section A but with the mandatory @root-blank snapshot step between disko run and nixos-install.
2. Re-letter subsequent sections: existing B → C, existing C → D.
3. Update the Profiles table — remove vm row; update host/server descriptions to reflect btrfs/lvm layout.
4. Update the Repository Layout section — replace stale disko-configuration.nix with modules/system/disko.nix; add/update impermanence.nix description; update boot.nix description.
5. Update "What NERV provides" impermanence line — replace "minimal" mode reference with "btrfs" mode (BTRFS rollback-on-boot).
6. Update the impermanence.nix Module Reference table — mode column shows "btrfs"│"full" (not "minimal"│"full").

Purpose: PROF-04. The @root-blank snapshot step is mandatory before nixos-install for BTRFS hosts — without it the Phase 10 rollback service fails on first boot with no source subvolume. The README is the primary operator-facing documentation; stale references to removed files and invalid enum values mislead users.

Output: README.md with accurate install procedure, profiles, and repository layout.
</objective>

<execution_context>
@/Users/nemesixsrl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nemesixsrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-profile-wiring-and-documentation/12-CONTEXT.md

<interfaces>
<!-- Current README.md structure — read 2026-03-10. Relevant sections shown. -->

Current "What NERV provides" impermanence line (line 28):
  "Impermanence — `minimal` mode mounts `/tmp` and `/var/tmp` as tmpfs; `full` mode wipes `/` on reboot and persists state to `/persist`"

Current Profiles table (lines 43–47):
  | `host`   | Desktop / laptop | Audio, Bluetooth, printing, Lanzaboote, minimal impermanence |
  | `server` | Headless server  | SSH only, full impermanence (`/` as tmpfs, state on `/persist`) |
  | `vm`     | Virtual machine  | Like host but no Bluetooth, printing, or Secure Boot (no TPM2) |

Section A title (line 52): "### A — New system (NixOS Live ISO)"
Section A references "hosts/disko-configuration.nix" at lines 54 and 65 — LEAVE SECTION A UNCHANGED.

Section B title (current line 92): "### B — Existing NixOS system"  → becomes ### C
Section C title (current line 112): "### C — Enabling Secure Boot"  → becomes ### D

Current impermanence.nix Module Reference table (line 341 area):
  | `nerv.impermanence.mode` | `"minimal"│"full"` | `"minimal"` | `minimal` = ... ; `full` = ... |

Current Repository Layout (lines 431–457):
  The tree shows:
    hosts/
    │   ├── configuration.nix
    │   ├── disko-configuration.nix    ← STALE (should be modules/system/disko.nix)
    │   └── hardware-configuration.nix
    modules/system/
    │   ├── boot.nix         # LUKS/initrd/bootloader       ← update description
    │   └── impermanence.nix # tmpfs mounts, full server mode  ← update description
  Note: impermanence.nix IS already listed in the tree; disko.nix is NOT.

Section A current steps (lines 64–79) for reference — DO NOT CHANGE:
  # 3. Edit disko-configuration.nix — replace disk, swap, store, and persist sizes.
  nano hosts/disko-configuration.nix
  # 4. Provision the disk (THIS WILL ERASE THE TARGET DISK).
  nix --experimental-features "nix-command flakes" run github:nix-community/disko/v1.13.0 -- \
    --mode destroy,format,mount hosts/disko-configuration.nix
  # 5. Generate hardware configuration and copy it into place.
  nixos-generate-config --no-filesystems --root /mnt
  # 7. Install.
  nixos-install --flake /mnt/etc/nixos#host   # or #server / #vm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert BTRFS install section; re-letter B→C and C→D; update Profiles table and impermanence description</name>
  <files>README.md</files>
  <action>
    Read README.md in full before editing.

    CHANGE 1 — "What NERV provides" impermanence bullet (line ~28):
    Replace:
      "Impermanence — `minimal` mode mounts `/tmp` and `/var/tmp` as tmpfs; `full` mode wipes `/` on reboot and persists state to `/persist`"
    With:
      "Impermanence — `btrfs` mode uses BTRFS rollback to reset `/` on every boot, persisting state to `/persist` (@persist subvolume); `full` mode wipes `/` on reboot and persists state to `/persist` (LVM)"

    CHANGE 2 — Profiles table:
    Replace the existing three-row table with:
    ```
    | Profile | Use case | Key differences |
    |---------|----------|-----------------|
    | `host`   | Desktop / laptop | Audio, Bluetooth, printing, Lanzaboote, BTRFS impermanence (rollback on boot) |
    | `server` | Headless server  | SSH only, LVM layout, full impermanence (`/` as tmpfs, state on `/persist`) |
    ```
    (Remove the `vm` row entirely.)

    CHANGE 3 — Insert new section B after Section A's closing `---` separator and before current Section B.
    The new section B content:

    ```markdown
    ### B — BTRFS layout (hostProfile)

    > **Before you begin:** find your disk name (`lsblk -d -o NAME,SIZE,MODEL`). NERV uses `modules/system/disko.nix` — the disk layout is configured via `nerv.disko.layout = "btrfs"` in `hostProfile` (already set). Fill `hosts/configuration.nix` with your machine-specific values.

    > **Warning:** The step that creates `@root-blank` (step 5 below) is mandatory. The initrd rollback service snapshots `@root-blank → @` on every boot. Skipping step 5 causes first-boot failure: the rollback service exits with an error because `@root-blank` does not exist.

    ```bash
    # 1. Boot the NixOS minimal ISO and get a root shell.

    # 2. Clone NERV.nixos into the standard NixOS config location.
    mkdir -p /mnt/etc/nixos
    git clone https://github.com/atirelli3/NERV.nixos.git /mnt/etc/nixos
    cd /mnt/etc/nixos

    # 3. Mount the BTRFS install volume temporarily (disko will handle permanent mounts).
    #    Nothing to edit in disko.nix — the BTRFS layout is fully declared.
    #    Set the disk device in hosts/configuration.nix (step 6).

    # 4. Provision the disk (THIS WILL ERASE THE TARGET DISK).
    nix --experimental-features "nix-command flakes" run github:nix-community/disko/v1.13.0 -- \
      --mode destroy,format,mount modules/system/disko.nix

    # 5. Create the @root-blank rollback baseline — MANDATORY before nixos-install.
    #    The initrd rollback service (Phase 10) snapshots @root-blank → @ on every boot.
    #    This step creates the clean baseline. Skipping it breaks the first boot.
    btrfs subvolume snapshot -r /mnt/@ /mnt/@root-blank

    # 6. Generate hardware configuration and copy it into place.
    nixos-generate-config --no-filesystems --root /mnt
    cp /mnt/etc/nixos/hardware-configuration.nix hosts/hardware-configuration.nix

    # 7. Edit hosts/configuration.nix — fill every PLACEHOLDER value.
    nano hosts/configuration.nix

    # 8. Install.
    nixos-install --flake /mnt/etc/nixos#host
    ```

    After the first boot, NERV lives at `/etc/nixos`. Apply future changes with:

    ```bash
    nixos-rebuild switch --flake /etc/nixos#host
    # If Home Manager is enabled (nerv.home.enable = true):
    nixos-rebuild switch --flake /etc/nixos#host --impure
    ```

    ---
    ```

    CHANGE 4 — Re-letter sections:
    - "### B — Existing NixOS system" → "### C — Existing NixOS system"
    - "### C — Enabling Secure Boot" → "### D — Enabling Secure Boot"
    All cross-references within each section body remain unchanged (they have no lettered references internally).

    IMPORTANT: Do NOT modify Section A content in any way.
  </action>
  <verify>
    <automated>
      cd /Users/nemesixsrl/Developemnt/NERV.nixos &&
      grep -q 'BTRFS layout (hostProfile)' README.md &&
      grep -q 'root-blank' README.md &&
      ! grep -q '"minimal"' README.md &&
      ! grep -q '`vm`' README.md &&
      grep -q '### C — Existing NixOS system' README.md &&
      grep -q '### D — Enabling Secure Boot' README.md &&
      echo "PASS"
    </automated>
  </verify>
  <done>
    README.md Section B contains the BTRFS install walkthrough with the @root-blank snapshot step (step 5).
    The Profiles table has only host and server rows. The impermanence description uses "btrfs" mode.
    Former B is now C, former C is now D.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Repository Layout section and impermanence.nix Module Reference table</name>
  <files>README.md</files>
  <action>
    Read README.md in full (after Task 1 changes are in place).

    CHANGE 5 — Repository Layout tree:
    Find the Repository Layout section (## Repository Layout). In the tree:

    a) Remove the line:
       `│   ├── disko-configuration.nix  # declarative disk layout — edit this`
       from under `hosts/`.

    b) In `modules/system/`, find:
       `│   ├── boot.nix             # LUKS/initrd/bootloader`
       Replace with:
       `│   ├── boot.nix             # layout-agnostic initrd + bootloader`

    c) In `modules/system/`, find:
       `│   └── impermanence.nix     # tmpfs mounts, full server mode`
       Replace with:
       `│   ├── disko.nix            # disk layout (btrfs/lvm), LUKS, initrd services`
       `│   └── impermanence.nix     # BTRFS or full impermanence mode`

    d) Update `flake.nix` description line from:
       `# inputs, profiles, nixosModules, nixosConfigurations`
       to:
       `# inputs, profiles (host/server), nixosModules, nixosConfigurations`

    CHANGE 6 — impermanence.nix Module Reference table (under "#### `impermanence.nix`"):
    Find the row:
      `| `nerv.impermanence.mode` | `"minimal"│"full"` | `"minimal"` | `minimal` = `/tmp`+`/var/tmp` as tmpfs; `full` = `/` as tmpfs, state on `/persist` |`
    Replace with:
      `| `nerv.impermanence.mode` | `"btrfs"│"full"` | **required** | `btrfs` = BTRFS rollback resets `/` on each boot, state on `/persist` (@persist subvolume); `full` = `/` as tmpfs, state on `/persist` (LVM) |`

    Also update the prose below the table that says:
      "Full mode persists: `/var/log`, `/var/lib/nixos`..."
    to accurately reflect btrfs mode persists (not full mode — full mode is tmpfs):
      Keep the "Full mode persists" line as-is (it describes the full mode correctly).
      Add a line before it:
      "BTRFS mode persists (via `environment.persistence`): `/var/lib/nixos`, `/var/lib/systemd`, `/etc/nixos`, SSH host keys, and `machine-id`. `/var/log` is excluded (persisted by the @log BTRFS subvolume)."

    Also update the note below the impermanence table:
      Find: "> Full mode requires `impermanence.nixosModules.impermanence`..."
      Change to: "> Both `btrfs` and `full` modes require `impermanence.nixosModules.impermanence` in the host's modules list (pre-wired in both the `host` and `server` profiles)."

    No other changes to README.md.
  </action>
  <verify>
    <automated>
      cd /Users/nemesixsrl/Developemnt/NERV.nixos &&
      grep -q 'modules/system/disko.nix\|disko\.nix.*disk layout' README.md &&
      ! grep -q 'hosts/disko-configuration.nix' README.md &&
      grep -q 'layout-agnostic initrd' README.md &&
      grep -q '"btrfs".*"full"' README.md &&
      echo "PASS"
    </automated>
  </verify>
  <done>
    Repository Layout tree shows modules/system/disko.nix and removes hosts/disko-configuration.nix.
    boot.nix description reads "layout-agnostic initrd + bootloader".
    impermanence.nix Module Reference table shows "btrfs"│"full" (not "minimal"│"full").
    hosts/disko-configuration.nix no longer appears anywhere in README.md.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- grep "BTRFS layout (hostProfile)" README.md returns a match
- grep "root-blank" README.md returns at least 2 matches (Section B step + disko.nix reference)
- grep "nixos-install" README.md — Section B shows nixos-install as the LAST step after @root-blank
- grep '"minimal"' README.md returns 0 (no stale mode references)
- grep '`vm`' README.md returns 0 (vm profile row gone from table)
- grep "### C — Existing NixOS system" README.md returns 1 match
- grep "### D — Enabling Secure Boot" README.md returns 1 match
- grep "hosts/disko-configuration.nix" README.md returns 0 (stale path gone)
- grep "layout-agnostic initrd" README.md returns 1 match (boot.nix description)
- grep "disko.nix" README.md returns matches (Repository Layout)
</verification>

<success_criteria>
- README.md has a complete BTRFS install walkthrough (Section B) with @root-blank snapshot as step 5, before nixos-install (PROF-04)
- Step ordering is explicit: disko run (step 4) → @root-blank snapshot (step 5, mandatory) → nixos-install (step 8)
- Warning callout explains why @root-blank cannot be skipped
- Profiles table has two rows: host (BTRFS impermanence) and server (LVM + full impermanence)
- Repository Layout shows modules/system/disko.nix; hosts/disko-configuration.nix is removed
- impermanence.nix Module Reference table shows "btrfs"│"full" enum
- "minimal" mode string does not appear anywhere in README.md
</success_criteria>

<output>
After completion, create `.planning/phases/12-profile-wiring-and-documentation/12-03-SUMMARY.md`
</output>
