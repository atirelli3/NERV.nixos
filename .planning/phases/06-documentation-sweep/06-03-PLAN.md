---
phase: 06-documentation-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - hosts/nixos-base/configuration.nix
  - hosts/nixos-base/disko-configuration.nix
  - hosts/nixos-base/hardware-configuration.nix
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "disko-configuration.nix opens with a prominent WARNING block listing /dev/DISK and SIZE_RAM * 2 placeholders before any Nix expression"
    - "disko-configuration.nix header includes NIXLUKS cross-reference naming modules/system/boot.nix"
    - "configuration.nix opens with a structured header describing its role as the host's nerv.* declaration surface"
    - "hardware-configuration.nix opens with a structured placeholder-convention header"
    - "All three host files parse without errors after edits"
  artifacts:
    - path: "hosts/nixos-base/disko-configuration.nix"
      provides: "DOCS-01 header + DOCS-03 warning block + DOCS-04 LUKS cross-reference"
      contains: "!! WARNING"
    - path: "hosts/nixos-base/configuration.nix"
      provides: "DOCS-01 header + DOCS-02 inline comment review"
      contains: "# hosts/nixos-base/configuration.nix"
    - path: "hosts/nixos-base/hardware-configuration.nix"
      provides: "DOCS-01 structured placeholder header"
      contains: "# hosts/nixos-base/hardware-configuration.nix"
  key_links:
    - from: "hosts/nixos-base/disko-configuration.nix header"
      to: "modules/system/boot.nix"
      via: "LUKS cross-reference comment in header"
      pattern: "NIXLUKS.*boot\\.nix"
    - from: "hosts/nixos-base/disko-configuration.nix header"
      to: "placeholder values /dev/DISK and SIZE_RAM * 2"
      via: "WARNING block listing both placeholders"
      pattern: "/dev/DISK"
---

<objective>
Add documentation headers to all three files in hosts/nixos-base/: the disko-configuration.nix warning+LUKS block (DOCS-03 and DOCS-04), the configuration.nix host-role header (DOCS-01), and the hardware-configuration.nix placeholder-convention header (DOCS-01).

Purpose: Satisfies the remaining DOCS-01 scope in hosts/nixos-base/ and fully closes DOCS-03 and DOCS-04.
Output: Three host files with correct headers; disko-configuration.nix becomes safe for new adopters with prominent placeholder warnings.
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documentation-sweep/06-RESEARCH.md

@hosts/nixos-base/configuration.nix
@hosts/nixos-base/disko-configuration.nix
@hosts/nixos-base/hardware-configuration.nix
</context>

<interfaces>
<!-- disko-configuration.nix: requires BOTH the WARNING block AND the purpose header. -->
<!-- They combine into a single header block at the very top, before the opening `{`. -->

```nix
# hosts/nixos-base/disko-configuration.nix
#
# !! WARNING — PLACEHOLDER VALUES MUST BE REPLACED BEFORE USE !!
#
#   /dev/DISK      Replace with the actual target disk, e.g. "/dev/nvme0n1"
#                  Find with: lsblk -d -o NAME,SIZE,MODEL
#   SIZE_RAM * 2   Replace with a concrete size, e.g. "16G" (2× physical RAM).
#                  Find RAM size with: free -h
#
# Purpose  : Disko declarative disk layout — GPT / EFI / LUKS-on-LVM.
# Options  : None — edit this file directly for the target machine.
# LUKS     : NIXLUKS label must stay in sync with modules/system/boot.nix
#            and modules/system/secureboot.nix.
```

<!-- configuration.nix: host-role header from RESEARCH.md Code Examples. -->
<!-- The file has NO existing header. Key entry points are all the nerv.* options it sets. -->

```nix
# hosts/nixos-base/configuration.nix
#
# Purpose  : Machine-specific configuration for the nixos-base host.
# Role     : Declares nerv.* options for this machine; does not define module logic.
# Entry    : nerv.hostname, nerv.locale.*, nerv.primaryUser, nerv.hardware.*,
#            nerv.openssh.*, nerv.audio.enable, nerv.bluetooth.enable,
#            nerv.printing.enable, nerv.zsh.enable, nerv.home.*
# Override : Edit this file directly. This is the user-facing configuration surface.
# Note     : fileSystems use lib.mkForce to override Disko-generated mounts.
#            Labels (NIXBOOT, NIXROOT, NIXSWAP) must stay in sync with
#            hosts/nixos-base/disko-configuration.nix.
```

<!-- hardware-configuration.nix: brief placeholder header. The file body is `{ ... }: { }`. -->
<!-- Current state: # PLACEHOLDER — replace with output of: nixos-generate-config --show-hardware-config -->
<!-- Replace the inline comment with a structured header. -->

```nix
# hosts/nixos-base/hardware-configuration.nix
#
# Purpose  : Placeholder — real hardware configuration is machine-specific.
# Override : On the target NixOS machine, replace this file with the output of:
#              nixos-generate-config --show-hardware-config
#            Or copy /etc/nixos/hardware-configuration.nix after running nixos-generate-config.
# Note     : Do not describe any real hardware here; this placeholder ships with the repo
#            and is replaced per-machine.
```

<!-- Existing state of disko-configuration.nix body (verified by read): -->
<!-- Line 4:  device = "/dev/DISK";  # replace with actual disk, e.g. "/dev/nvme0n1" -->
<!-- Line 26: extraFormatArgs = [ "--label" "NIXLUKS" ];  # NIXLUKS label — must stay in sync with modules/system/boot.nix and modules/system/secureboot.nix -->
<!-- Line 42: size = "SIZE_RAM * 2";  # placeholder — replace with e.g. "16G" (2× RAM) -->
<!-- Line 27: passwordFile = "/tmp/luks-password";  # pre-seeded by the install script -->
<!-- Body inline comments are already present — header addition is the only change needed. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add WARNING+LUKS header to disko-configuration.nix (DOCS-03, DOCS-04)</name>
  <files>hosts/nixos-base/disko-configuration.nix</files>
  <action>
Read the file. The file currently starts at line 1 with `{` (no header). Prepend the combined WARNING block + purpose header shown in the interfaces section above, before the opening `{`.

The exact block to add at the very top:

```
# hosts/nixos-base/disko-configuration.nix
#
# !! WARNING — PLACEHOLDER VALUES MUST BE REPLACED BEFORE USE !!
#
#   /dev/DISK      Replace with the actual target disk, e.g. "/dev/nvme0n1"
#                  Find with: lsblk -d -o NAME,SIZE,MODEL
#   SIZE_RAM * 2   Replace with a concrete size, e.g. "16G" (2× physical RAM).
#                  Find RAM size with: free -h
#
# Purpose  : Disko declarative disk layout — GPT / EFI / LUKS-on-LVM.
# Options  : None — edit this file directly for the target machine.
# LUKS     : NIXLUKS label must stay in sync with modules/system/boot.nix
#            and modules/system/secureboot.nix.
```

Do NOT modify the body of the file — the existing inline comment on line 26 (`# NIXLUKS label — must stay in sync with...`) is part of DOCS-04 body compliance and must be preserved.

After editing, run `nix-instantiate --parse hosts/nixos-base/disko-configuration.nix`.

Verify DOCS-03: `head -14 hosts/nixos-base/disko-configuration.nix` shows the WARNING block.
Verify DOCS-04: `grep -n "NIXLUKS" hosts/nixos-base/disko-configuration.nix` returns both the header LUKS line and the body inline comment on line 26+.
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix-instantiate --parse hosts/nixos-base/disko-configuration.nix && grep -c "NIXLUKS" hosts/nixos-base/disko-configuration.nix</automated>
  </verify>
  <done>File parses cleanly. `grep -c NIXLUKS` returns 2 or more (header LUKS line + existing body inline comment). `head -14` shows the WARNING block prominently at the top.</done>
</task>

<task type="auto">
  <name>Task 2: Add headers to configuration.nix and hardware-configuration.nix (DOCS-01, DOCS-02)</name>
  <files>hosts/nixos-base/configuration.nix, hosts/nixos-base/hardware-configuration.nix</files>
  <action>
For configuration.nix:
The file starts with `{ config, lib, pkgs, ... }:` with no header. Prepend the host-role header from the interfaces section above before the opening `{ config, lib, pkgs, ... }:`.

After adding the header, review each `nerv.*` block in the file body for DOCS-02 inline comment clarity:
- `networking.networkmanager.enable = true` — no comment needed; self-evident
- `fileSystems` block — already has `# lib.mkForce overrides the Disko-generated mounts — keep labels in sync with disko-configuration.nix.` — leave it; it satisfies DOCS-02
- `nerv.hardware.cpu = "amd"` — already has trailing inline comment explaining AMD/IOMMU — leave it
- `nerv.hardware.gpu = "none"` — already has trailing comment — leave it
- `nerv.openssh` block — `# port defaults to 2222; tarpitPort defaults to 22` comment already present — leave it
- `nerv.audio.enable = false` through `nerv.bluetooth.enable = false` — add brief comment if not present: `# enable when on target NixOS machine with audio hardware`
- `nerv.home.users` line — already has comment `# demon0 must have ~/home.nix on target system` — leave it

For hardware-configuration.nix:
The file currently has a single inline comment: `# PLACEHOLDER — replace with output of: nixos-generate-config --show-hardware-config`. Remove that inline comment and replace the entire file with the structured header plus the unchanged `{ ... }: { }` body:

```nix
# hosts/nixos-base/hardware-configuration.nix
#
# Purpose  : Placeholder — real hardware configuration is machine-specific.
# Override : On the target NixOS machine, replace this file with the output of:
#              nixos-generate-config --show-hardware-config
#            Or copy /etc/nixos/hardware-configuration.nix after running nixos-generate-config.
# Note     : Do not describe any real hardware here; this placeholder ships with the repo
#            and is replaced per-machine.
{ ... }: { }
```

After editing both files, run nix-instantiate --parse on each.
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix-instantiate --parse hosts/nixos-base/configuration.nix && nix-instantiate --parse hosts/nixos-base/hardware-configuration.nix</automated>
  </verify>
  <done>Both files parse cleanly. configuration.nix opens with `# hosts/nixos-base/configuration.nix` and the Role/Entry/Override/Note header. hardware-configuration.nix has a structured placeholder header followed by `{ ... }: { }`.</done>
</task>

</tasks>

<verification>
All three hosts/nixos-base/ files have compliant headers.

Spot-checks:
```bash
head -15 hosts/nixos-base/disko-configuration.nix   # WARNING block must be visible
grep -n "NIXLUKS" hosts/nixos-base/disko-configuration.nix  # must show header + body cross-refs
grep -n "NIXLUKS" modules/system/boot.nix            # must already show cross-ref (pre-existing)
head -12 hosts/nixos-base/configuration.nix          # must show host-role header
head -10 hosts/nixos-base/hardware-configuration.nix # must show placeholder header
```

Full build (validates no syntax errors in the host configuration):
```bash
nixos-rebuild build --flake /home/demon/Developments/test-nerv.nixos#nixos-base
```
</verification>

<success_criteria>
- disko-configuration.nix: WARNING block visible at top, NIXLUKS appears in both header and body, file parses cleanly
- configuration.nix: structured host-role header present, file parses cleanly
- hardware-configuration.nix: placeholder-convention header present, `{ ... }: { }` body intact, file parses cleanly
- DOCS-01 fully satisfied for hosts/nixos-base/
- DOCS-03 satisfied: prominent WARNING block with /dev/DISK and SIZE_RAM * 2 listed
- DOCS-04 satisfied: NIXLUKS cross-referenced in disko-configuration.nix header naming boot.nix
- Full build succeeds (no syntax regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-sweep/06-03-SUMMARY.md`
</output>
