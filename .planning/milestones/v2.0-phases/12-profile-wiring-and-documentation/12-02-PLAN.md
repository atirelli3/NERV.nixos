---
phase: 12-profile-wiring-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/system/disko.nix
  - modules/system/boot.nix
  - modules/system/impermanence.nix
  - hosts/configuration.nix
autonomous: true
requirements:
  - PROF-03

must_haves:
  truths:
    - "disko.nix header contains a Profiles cross-reference line naming hostProfile and serverProfile with their layout values"
    - "boot.nix header contains a Profiles cross-reference noting layout-conditional initrd lives in disko.nix"
    - "impermanence.nix header contains a Profiles cross-reference line naming hostProfile and serverProfile with their mode values"
    - "hosts/configuration.nix header Role line no longer mentions vmProfile"
  artifacts:
    - path: "modules/system/disko.nix"
      provides: "Updated header with Profiles cross-reference"
      contains: "Profiles"
    - path: "modules/system/boot.nix"
      provides: "Updated header with Profiles note"
      contains: "Profiles"
    - path: "modules/system/impermanence.nix"
      provides: "Updated header with Profiles cross-reference"
      contains: "Profiles"
    - path: "hosts/configuration.nix"
      provides: "Header Role line without vmProfile reference"
  key_links:
    - from: "disko.nix header Profiles line"
      to: "flake.nix hostProfile nerv.disko.layout = btrfs"
      via: "comment cross-reference"
      pattern: "Profiles.*hostProfile"
    - from: "impermanence.nix header Profiles line"
      to: "flake.nix hostProfile nerv.impermanence.mode = btrfs"
      via: "comment cross-reference"
      pattern: "Profiles.*hostProfile"
---

<objective>
Update section-header comment blocks in three module files and fix the Role line in hosts/configuration.nix:

1. modules/system/disko.nix — append a `# Profiles :` line after the existing `# LUKS :` section
2. modules/system/boot.nix — append a `# Profiles :` line after the existing `# Note :` section
3. modules/system/impermanence.nix — append a `# Profiles :` line after the existing `# Note :` section
4. hosts/configuration.nix — remove "or vmProfile" from the Role line

Purpose: PROF-03. Module headers are the in-repo documentation for operators who read source to understand the system. After Phases 9–11, the headers are accurate about options but silent about which profile uses which mode. Adding a Profiles cross-reference line makes the profile→module relationship navigable from the module side.

Output: Four files with updated comments; no logic changes to any file.
</objective>

<execution_context>
@/Users/nemesixsrl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nemesixsrl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-profile-wiring-and-documentation/12-CONTEXT.md

<interfaces>
<!-- Exact current headers — read 2026-03-10. Only the header comment blocks are shown. -->

modules/system/disko.nix header (lines 1–23):
```
# modules/system/disko.nix
#
# Purpose  : Declarative disk layout (GPT / EFI / LUKS) and layout-conditional initrd
#            configuration. Supports two layout types controlled by nerv.disko.layout
#            (no default — must be set explicitly):
#              btrfs — GPT/LUKS/BTRFS with subvolumes (desktop/laptop);
#                      initrd includes btrfs-progs and a systemd rollback service that
#                      deletes @ and re-snapshots @root-blank → @ on every boot.
#              lvm   — GPT/LUKS/LVM with swap, /nix, /persist LVs (server);
#                      initrd enables LVM scanning and dm-snapshot kernel module.
#            The disk device is set independently via disko.devices.disk.main.device
#            in hosts/configuration.nix and merged here by the module system.
#            LUKS unlock (boot.initrd.luks.devices."cryptroot") is declared here and
#            applies to both layouts — layout-agnostic bootloader settings are in boot.nix.
# Options  : nerv.disko.layout (enum, no default)
#            nerv.disko.lvm.swapSize, nerv.disko.lvm.storeSize, nerv.disko.lvm.persistSize
#            boot.initrd.supportedFilesystems (btrfs branch)
#            boot.initrd.systemd.storePaths (btrfs branch)
#            boot.initrd.systemd.services.rollback (btrfs branch)
#            boot.initrd.services.lvm.enable (lvm branch)
# LUKS     : NIXLUKS label must stay in sync with modules/system/secureboot.nix.
#            @root-blank must be created manually after disko run, before nixos-install:
#              btrfs subvolume snapshot -r /mnt/@ /mnt/@root-blank
```

modules/system/boot.nix header (lines 1–10):
```
# modules/system/boot.nix
#
# Purpose : Layout-agnostic initrd and bootloader configuration. Enables systemd stage 1
#           (boot.initrd.systemd.enable) and configures systemd-boot + EFI. All layout-
#           specific initrd config (BTRFS rollback service, LVM lvm.enable, LUKS unlock)
#           lives in modules/system/disko.nix.
# Options : None — fully opaque. Use lib.mkForce to override any setting.
# Note    : boot.kernelPackages = pkgs.linuxPackages_latest is set here but
#           overridden by kernel.nix (lib.mkForce pkgs.linuxPackages_zen) —
#           kernel.nix is the authoritative source for the kernel package.
```

modules/system/impermanence.nix header (lines 1–12):
```
# modules/system/impermanence.nix
#
# Purpose  : Selective persistence via environment.persistence bind-mounts from /persist.
#            btrfs mode — for desktop/laptop: BTRFS rollback resets root on each boot;
#            /persist (@persist subvolume) holds state. No tmpfs /.
#            full mode  — for server: / as tmpfs; /persist holds state.
# Options  : nerv.impermanence.enable, mode, persistPath, extraDirs, users
# Defaults : enable = false; persistPath = "/persist"; extraDirs = []; users = {}
#            mode has NO default — must be declared explicitly per host.
# Override : lib.mkForce on any fileSystems.* or environment.persistence entry.
# Note     : Both modes require impermanence.nixosModules.impermanence in the host's
#            modules list for environment.persistence to function.
```

hosts/configuration.nix header Role line (current, line 5–6):
```
# Role     : Declares machine-specific values only. All service and feature
#            settings are controlled by the profile in flake.nix (hostProfile,
#            serverProfile, or vmProfile).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profiles cross-reference to disko.nix and boot.nix headers</name>
  <files>modules/system/disko.nix, modules/system/boot.nix</files>
  <action>
    For modules/system/disko.nix:
    Read the file in full. Append a `# Profiles :` line immediately after the last line of the
    existing `# LUKS :` section (the `#   btrfs subvolume snapshot -r /mnt/@ /mnt/@root-blank` line),
    before the blank line that precedes `{ config, lib, pkgs, ... }:`.

    Insert this block (maintaining the same `#` indentation style as the rest of the header):
    ```
    # Profiles : hostProfile   → nerv.disko.layout = "btrfs"  (see flake.nix)
    #            serverProfile → nerv.disko.layout = "lvm"    (see flake.nix)
    ```

    For modules/system/boot.nix:
    Read the file in full. Append a `# Profiles :` line immediately after the last line of the
    existing `# Note :` section (the `#           kernel.nix is the authoritative source...` line),
    before the blank line that precedes `{ pkgs, ... }:`.

    Insert this block:
    ```
    # Profiles : Used by both hostProfile (btrfs layout) and serverProfile (lvm layout).
    #            Layout-conditional initrd config lives in disko.nix, not here.
    ```

    No other changes to either file. Logic, options, and config blocks are untouched.
  </action>
  <verify>
    <automated>
      cd /Users/nemesixsrl/Developemnt/NERV.nixos &&
      grep -q 'Profiles' modules/system/disko.nix &&
      grep -q 'Profiles' modules/system/boot.nix &&
      grep -q 'hostProfile' modules/system/disko.nix &&
      grep -q 'hostProfile' modules/system/boot.nix &&
      echo "PASS"
    </automated>
  </verify>
  <done>
    disko.nix header ends with a Profiles cross-reference naming hostProfile (btrfs) and serverProfile (lvm).
    boot.nix header ends with a Profiles note referencing disko.nix for layout-conditional config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Profiles cross-reference to impermanence.nix; fix configuration.nix Role line</name>
  <files>modules/system/impermanence.nix, hosts/configuration.nix</files>
  <action>
    For modules/system/impermanence.nix:
    Read the file in full. Append a `# Profiles :` line immediately after the last line of the
    existing `# Note :` section (the `#            modules list for environment.persistence to function.` line),
    before the blank line that precedes `{ config, lib, pkgs, ... }:`.

    Insert this block:
    ```
    # Profiles : hostProfile   → mode = "btrfs"  (BTRFS rollback, /persist is @persist subvolume)
    #            serverProfile → mode = "full"   (/ as tmpfs, /persist is LV)
    ```

    For hosts/configuration.nix:
    Read the file in full. Find the Role line:
    ```
    # Role     : Declares machine-specific values only. All service and feature
    #            settings are controlled by the profile in flake.nix (hostProfile,
    #            serverProfile, or vmProfile).
    ```
    Replace it with:
    ```
    # Role     : Declares machine-specific values only. All service and feature
    #            settings are controlled by the profile in flake.nix (hostProfile
    #            or serverProfile).
    ```
    (Remove ", or vmProfile" from the last line of the Role block; adjust the comma before "or" accordingly.)

    No other changes to either file.
  </action>
  <verify>
    <automated>
      cd /Users/nemesixsrl/Developemnt/NERV.nixos &&
      grep -q 'Profiles' modules/system/impermanence.nix &&
      grep -q 'hostProfile' modules/system/impermanence.nix &&
      ! grep -q 'vmProfile' hosts/configuration.nix &&
      echo "PASS"
    </automated>
  </verify>
  <done>
    impermanence.nix header ends with a Profiles cross-reference naming hostProfile (mode=btrfs) and serverProfile (mode=full).
    hosts/configuration.nix Role line reads "hostProfile or serverProfile" — no vmProfile reference.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- grep "Profiles" in all three module files returns matches
- grep "hostProfile" in disko.nix, boot.nix, impermanence.nix returns matches
- grep "vmProfile" in hosts/configuration.nix returns no matches
- No logic changes to any file (only header comments modified)
</verification>

<success_criteria>
- disko.nix, boot.nix, impermanence.nix each contain a "# Profiles :" header line cross-referencing hostProfile and serverProfile (PROF-03)
- hosts/configuration.nix Role line mentions only hostProfile and serverProfile — vmProfile reference removed
- No Nix code was modified in any of the four files
</success_criteria>

<output>
After completion, create `.planning/phases/12-profile-wiring-and-documentation/12-02-SUMMARY.md`
</output>
