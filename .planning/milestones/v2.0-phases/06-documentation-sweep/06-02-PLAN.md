---
phase: 06-documentation-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/services/default.nix
  - modules/system/default.nix
  - modules/default.nix
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "modules/services/default.nix opens with a structured aggregator header"
    - "modules/system/default.nix has a structured header replacing its minimal one-liner"
    - "modules/default.nix has a structured header replacing its minimal two-line comment"
    - "All three aggregator files parse without errors after edits"
  artifacts:
    - path: "modules/services/default.nix"
      provides: "DOCS-01 aggregator header"
      contains: "# modules/services/default.nix"
    - path: "modules/system/default.nix"
      provides: "DOCS-01 aggregator header"
      contains: "# modules/system/default.nix"
    - path: "modules/default.nix"
      provides: "DOCS-01 aggregator header"
      contains: "# modules/default.nix"
  key_links:
    - from: "modules/default.nix header"
      to: "./system, ./services, ../home imports"
      via: "# Modules line listing all three subtrees"
      pattern: "# Modules.*system.*services.*home"
---

<objective>
Add structured section-header comment blocks to the three aggregator default.nix files: modules/services/default.nix, modules/system/default.nix, and modules/default.nix.

Purpose: Satisfies DOCS-01 for the aggregator files which currently have no structured headers.
Output: Three aggregator files each with a Purpose-only canonical header (aggregators have no options, no defaults, no override surface).
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-documentation-sweep/06-RESEARCH.md

@modules/services/default.nix
@modules/system/default.nix
@modules/default.nix
</context>

<interfaces>
<!-- Canonical aggregator header format — Purpose + Modules only, no Options/Defaults/Override -->

```nix
# modules/services/default.nix
#
# Purpose  : Aggregates all nerv service modules.
# Modules  : openssh, pipewire, bluetooth, printing, zsh
# Note     : All modules are disabled by default. Enable via nerv.<module>.enable.
```

<!-- Current file contents for reference: -->

modules/services/default.nix (7 lines):
  { imports = [ ./openssh.nix ./pipewire.nix ./bluetooth.nix ./printing.nix ./zsh.nix ]; }

modules/system/default.nix (12 lines):
  # Aggregates all nerv system modules.
  { imports = [ ./identity.nix ./hardware.nix ./kernel.nix ./security.nix ./nix.nix
                ./boot.nix ./impermanence.nix ./secureboot.nix ]; }
  (The existing one-liner comment becomes the structured header — do not lose import ordering notes)

modules/default.nix (4 lines):
  # Aggregates all nerv library modules.
  # system/ populated by Phase 3, services/ by Phase 2, home/ by Phase 5.
  # ../home resolves to <repo-root>/home (relative to this file's location).
  { imports = [ ./system ./services ../home ]; }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add structured headers to all three aggregator default.nix files</name>
  <files>modules/services/default.nix, modules/system/default.nix, modules/default.nix</files>
  <action>
Read each file, replace its existing comment (if any) with the canonical aggregator header, then run nix-instantiate --parse on each.

For modules/services/default.nix:
Replace the bare `{ imports = [...];}` with a header prepended before the opening `{`:
```
# modules/services/default.nix
#
# Purpose  : Aggregates all nerv service modules.
# Modules  : openssh, pipewire, bluetooth, printing, zsh
# Note     : All modules are disabled by default. Enable via nerv.<module>.enable.
```
Keep the imports block unchanged.

For modules/system/default.nix:
The file currently has `# Aggregates all nerv system modules.` as a one-liner. Replace with a full structured header:
```
# modules/system/default.nix
#
# Purpose  : Aggregates all nerv system modules.
# Modules  : identity, hardware, kernel, security, nix, boot, impermanence, secureboot
# Note     : secureboot.nix must be last — it applies lib.mkForce false on systemd-boot
#            to prevent conflict with Lanzaboote. Import order is significant.
```
Keep the existing imports block (with its inline comments on boot.nix, impermanence.nix, secureboot.nix) unchanged — those are valuable and count toward DOCS-02.

For modules/default.nix:
Replace the three existing comment lines with a structured header:
```
# modules/default.nix
#
# Purpose  : Top-level aggregator — imports all nerv module subtrees.
# Modules  : modules/system (system-level config), modules/services (service config),
#            home (Home Manager wiring)
# Note     : ../home resolves to <repo-root>/home relative to this file's location.
```
Keep the `{ imports = [ ./system ./services ../home ]; }` expression unchanged.

CRITICAL: All three files are aggregators with no options. Do NOT add Options, Defaults, or Override lines. The Note field is used only to capture import ordering constraints or path conventions.
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix-instantiate --parse modules/services/default.nix && nix-instantiate --parse modules/system/default.nix && nix-instantiate --parse modules/default.nix</automated>
  </verify>
  <done>All three aggregator files parse cleanly. Each opens with a `# modules/.../default.nix` line and structured Purpose/Modules/Note header. The imports expressions are unchanged.</done>
</task>

</tasks>

<verification>
All three aggregator files have structured headers compliant with DOCS-01.

Spot-check:
```
head -8 modules/services/default.nix  # must show structured header
head -10 modules/system/default.nix   # must show structured header
head -8 modules/default.nix           # must show structured header
```
</verification>

<success_criteria>
- modules/services/default.nix, modules/system/default.nix, modules/default.nix each open with the canonical comment format
- `nix-instantiate --parse` returns exit 0 for all three files
- Existing import ordering inline comments in modules/system/default.nix are preserved
- DOCS-01 satisfied for all three aggregator files
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-sweep/06-02-SUMMARY.md`
</output>
