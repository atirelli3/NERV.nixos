---
phase: 03-system-modules-non-boot
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - modules/system/security.nix
  - modules/system/nix.nix
  - modules/system/default.nix
  - hosts/nixos-base/configuration.nix
autonomous: true
requirements:
  - OPT-01
  - OPT-02
  - OPT-03
  - OPT-04

must_haves:
  truths:
    - "nixos-rebuild build --flake .#nixos-base succeeds with no evaluation errors after all migrations"
    - "modules/system/default.nix imports identity.nix, hardware.nix, kernel.nix, security.nix, and nix.nix"
    - "hosts/nixos-base/configuration.nix declares nerv.hostname, nerv.locale.*, nerv.primaryUser, nerv.hardware.cpu, and nerv.hardware.gpu via the nerv.* API"
    - "The stale /etc/nixos#nixos path in nix.nix is fixed to /etc/nerv#nixos-base"
    - "security.nix content is in modules/system/ and all hardening is active"
  artifacts:
    - path: "modules/system/security.nix"
      provides: "Opaque security hardening (AppArmor, auditd, ClamAV, AIDE)"
    - path: "modules/system/nix.nix"
      provides: "Nix daemon config with corrected autoUpgrade flake path"
    - path: "modules/system/default.nix"
      provides: "System module aggregator importing all five system modules"
      contains: "identity.nix"
    - path: "hosts/nixos-base/configuration.nix"
      provides: "Host config using nerv.* API for identity and hardware"
  key_links:
    - from: "modules/system/default.nix"
      to: "modules/system/identity.nix"
      via: "./identity.nix import"
      pattern: "\\.\/identity"
    - from: "hosts/nixos-base/configuration.nix"
      to: "nerv.hostname"
      via: "nerv.hostname = \"nixos-base\""
      pattern: "nerv\\.hostname"
    - from: "modules/system/nix.nix"
      to: "/etc/nerv#nixos-base"
      via: "system.autoUpgrade.flake"
      pattern: "nerv#nixos-base"
---

<objective>
Complete the Phase 3 migration: migrate security.nix and nix.nix (with path fix) into modules/system/, populate the system/default.nix aggregator, update hosts/nixos-base/configuration.nix to use the nerv.* API for identity and hardware, then verify the full build.

Purpose: Wires all system modules together and replaces inline config in configuration.nix with nerv.* option calls — completing the options API for OPT-01 through OPT-04.
Output: Full system builds cleanly with identity, locale, primary user, and hardware options declared via nerv.*.
</objective>

<execution_context>
@/home/demon/.claude/get-shit-done/workflows/execute-plan.md
@/home/demon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-modules-non-boot/03-CONTEXT.md
@.planning/phases/03-system-modules-non-boot/03-RESEARCH.md
@.planning/phases/03-system-modules-non-boot/03-01-SUMMARY.md
@.planning/phases/03-system-modules-non-boot/03-02-SUMMARY.md

<interfaces>
<!-- Files created in Wave 1 plans — executor does not need to re-read them, just import them. -->

modules/system/default.nix (current stub):
```nix
# Stub — populated by Phase 3 (system modules).
{ imports = []; }
```
Will become:
```nix
{ imports = [ ./identity.nix ./hardware.nix ./kernel.nix ./security.nix ./nix.nix ]; }
```

hosts/nixos-base/configuration.nix current inline identity (to be replaced with nerv.* calls):
  networking.hostName = "nixos-base";         → nerv.hostname = "nixos-base";
  time.timeZone = "Europe/Rome";              → nerv.locale.timeZone = "Europe/Rome";
  i18n.defaultLocale = "en_US.UTF-8";        → nerv.locale.defaultLocale = "en_US.UTF-8";
  console = { font = ...; packages = [...]; keyMap = "us-acentos"; };  → nerv.locale.keyMap = "us-acentos"; (font/packages now owned by identity.nix)
  users.users.demon0.extraGroups = [...];    → nerv.primaryUser = [ "demon0" ];
                                               (isNormalUser, home, uid etc. stay in configuration.nix)

networking.networkmanager.enable stays in configuration.nix (it is not an identity option).
system.stateVersion stays in configuration.nix.
The boot/fileSystems/swapDevices block stays in configuration.nix (Phase 4 work).
nerv.openssh.*, nerv.audio.enable, nerv.bluetooth.enable, nerv.printing.enable, nerv.zsh.enable stay as-is.

nix.nix path fix — single line:
  flake = "/etc/nixos#nixos";   →   flake = "/etc/nerv#nixos-base";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate security.nix and nix.nix into modules/system/</name>
  <files>modules/system/security.nix, modules/system/nix.nix</files>
  <action>
Create modules/system/security.nix and modules/system/nix.nix.

**modules/system/security.nix:**
Copy the entire content of modules/security.nix verbatim. Add a section-header comment at the very top before the Nix expression:
```
# modules/system/security.nix
#
# Purpose : Security hardening — fully opaque, always-on.
# Options : None. All settings are enforced unconditionally.
#           Use lib.mkForce at the host level as an escape hatch only.
# Includes: AppArmor, auditd (with baseline ruleset), ClamAV daemon+updater,
#           AIDE file integrity monitoring (daily check timer).
```
No other changes. Do NOT add options blocks. Do NOT modify any hardening config.

**modules/system/nix.nix:**
Copy the entire content of modules/nix.nix with exactly one change:
  - Line: `flake = "/etc/nixos#nixos";`
  - Replace with: `flake = "/etc/nerv#nixos-base";`

Add a section-header comment at the very top:
```
# modules/system/nix.nix
#
# Purpose : Nix daemon and nixpkgs configuration — fully opaque.
# Options : None. GC, optimise, and autoUpgrade settings are hardcoded.
#           autoUpgrade toggle is a v2 roadmap item (OPT-V2-01).
# Override: Use lib.mkForce at the host level to override any setting.
```
All other content migrates verbatim.
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix-instantiate --parse modules/system/security.nix && nix-instantiate --parse modules/system/nix.nix && grep -q "nerv#nixos-base" modules/system/nix.nix</automated>
  </verify>
  <done>Both files parse without error. modules/system/nix.nix contains "/etc/nerv#nixos-base" (grep exit 0). modules/system/security.nix has the full AIDE, ClamAV, AppArmor, auditd config.</done>
</task>

<task type="auto">
  <name>Task 2: Populate modules/system/default.nix and update hosts/nixos-base/configuration.nix</name>
  <files>modules/system/default.nix, hosts/nixos-base/configuration.nix</files>
  <action>
**modules/system/default.nix:**
Replace the stub with the populated aggregator:
```nix
# Aggregates all nerv system modules (non-boot).
# Boot/LUKS/initrd configuration lives in hosts/nixos-base/configuration.nix until Phase 4.
{ imports = [
    ./identity.nix
    ./hardware.nix
    ./kernel.nix
    ./security.nix
    ./nix.nix
  ];
}
```

**hosts/nixos-base/configuration.nix:**
Read the current file and apply the following targeted replacements. Keep every other line verbatim.

REMOVE these inline identity declarations (they are now owned by identity.nix via nerv.*):
- The `networking = { hostName = "nixos-base"; ... };` block — but KEEP `networkmanager.enable = true`.
  Replace with: `networking.networkmanager.enable = true;`
- `time.timeZone = "Europe/Rome";`
- `i18n.defaultLocale = "en_US.UTF-8";`
- The entire `console = { ... };` block (font, packages, keyMap)
- `users.users.demon0.extraGroups = [ "wheel" "networkmanager" ];`
  Simplify users.users.demon0 to only keep what identity.nix does NOT set:
  `users.users.demon0 = { isNormalUser = true; };`

ADD these nerv.* declarations (grouped with the existing nerv.* service options section):
```nix
# nerv system options — configured via modules/system/
nerv.hostname              = "nixos-base";
nerv.locale.timeZone       = "Europe/Rome";
nerv.locale.defaultLocale  = "en_US.UTF-8";
nerv.locale.keyMap         = "us-acentos";
nerv.primaryUser           = [ "demon0" ];
nerv.hardware.cpu          = "amd";   # AMD CPU — enables microcode + amd_iommu=on iommu=pt
nerv.hardware.gpu          = "none";  # no dedicated GPU on this machine; enable when applicable
```

Keep all other content unchanged:
- imports = [ ./hardware-configuration.nix ];
- fileSystems / swapDevices / boot block (Phase 4)
- networking.networkmanager.enable = true
- system.stateVersion = "25.11"
- nerv.openssh.*, nerv.audio.enable, nerv.bluetooth.enable, nerv.printing.enable, nerv.zsh.enable
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nix-instantiate --parse modules/system/default.nix && nix-instantiate --parse hosts/nixos-base/configuration.nix && grep -q "nerv.hostname" hosts/nixos-base/configuration.nix && grep -q "nerv.primaryUser" hosts/nixos-base/configuration.nix && grep -q "nerv.hardware.cpu" hosts/nixos-base/configuration.nix</automated>
  </verify>
  <done>Both files parse without error. configuration.nix contains nerv.hostname, nerv.primaryUser, nerv.hardware.cpu, nerv.hardware.gpu. modules/system/default.nix imports all five system modules. Inline identity fields (time.timeZone, i18n.defaultLocale, console block) are absent from configuration.nix.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full build passes</name>
  <files></files>
  <action>
Run the full NixOS build to confirm all system modules evaluate without errors. This is the phase gate.

Command: `nixos-rebuild build --flake /home/demon/Developments/test-nerv.nixos#nixos-base`

If the build fails:
1. Read the error message carefully
2. Common causes:
   - Old flat modules/hardware.nix or modules/kernel.nix still imported somewhere — check modules/default.nix does NOT import them (it imports only ./system and ./services)
   - Duplicate boot.kernelParams from amd_iommu if kernel.nix migration missed the removal — grep modules/system/kernel.nix for "amd_iommu"
   - users.users.demon0 attribute conflict if extraGroups declared in both identity.nix (via nerv.primaryUser) and configuration.nix — ensure configuration.nix only has `isNormalUser = true` for demon0
   - nerv.hostname missing if the configuration.nix edit left the option undeclared — ensure `nerv.hostname = "nixos-base";` is present
3. Fix the root cause and re-run the build before marking this task done

The build must exit 0 before this task is complete.
  </action>
  <verify>
    <automated>cd /home/demon/Developments/test-nerv.nixos && nixos-rebuild build --flake .#nixos-base</automated>
  </verify>
  <done>nixos-rebuild build exits 0 with no evaluation errors. The built system includes all five system modules. nerv.hostname, nerv.locale.*, nerv.primaryUser, nerv.hardware.cpu, nerv.hardware.gpu are all active.</done>
</task>

</tasks>

<verification>
nixos-rebuild build --flake .#nixos-base exits 0. All five system module files exist and parse. configuration.nix uses only nerv.* API for identity and hardware settings. The stale /etc/nixos#nixos path is gone from nix.nix.
</verification>

<success_criteria>
1. nixos-rebuild build --flake .#nixos-base succeeds with no evaluation errors
2. modules/system/default.nix imports identity.nix, hardware.nix, kernel.nix, security.nix, nix.nix
3. hosts/nixos-base/configuration.nix declares nerv.hostname = "nixos-base", nerv.locale.timeZone = "Europe/Rome", nerv.locale.keyMap = "us-acentos", nerv.primaryUser = [ "demon0" ], nerv.hardware.cpu = "amd", nerv.hardware.gpu = "none"
4. modules/system/nix.nix references /etc/nerv#nixos-base (not /etc/nixos#nixos)
</success_criteria>

<output>
After completion, create .planning/phases/03-system-modules-non-boot/03-03-SUMMARY.md
</output>
