# hosts/nixos-base/hardware-configuration.nix
#
# Purpose  : Placeholder — real hardware configuration is machine-specific.
# Override : On the target NixOS machine, replace this file with the output of:
#              nixos-generate-config --show-hardware-config
#            Or copy /etc/nixos/hardware-configuration.nix after running nixos-generate-config.
# Note     : Do not describe any real hardware here; this placeholder ships with the repo
#            and is replaced per-machine.
{ ... }: { }
